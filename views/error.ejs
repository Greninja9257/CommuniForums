<%- include('layout', { body: `
<div class="error-page">
  <h1>${title || 'Error'}</h1>
  <p>${message || 'Something went wrong.'}</p>
  <a href="/" class="btn btn-primary">Back to Home</a>
</div>
` }) %>
